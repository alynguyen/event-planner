<%- include('partials/header') %>

    <div class="container-main">
    <div class="d-flex flex-column login align-items-center justify-content-center">
      <img class="logo" src="images/eventb-logo.png"><br>
      <% if (!user) { %>
      <p class="signin">Sign in to Eventb</p>
      <% } else { %>
      <p class=""></p>
      <% } %>
        <% if (user) { %>
          <a href="/events/new" id="btn123" class="btn btn-idx btn-outline-secondary">New Event</a>
          <a href="/events/" class="btn btn-idx btn-outline-secondary">Events List</a>
          <a href="/logout" class="btn btn-idx btn-outline-secondary">Log Out</a>
          <% } else { %>
            <a href="/auth/google" class="btn btn-outline-secondary"><img class="glogo" src="images/g-logo.png">Sign in with Google</a>
          <% } %>
    </div>
    </div>
  </body>
</html>
